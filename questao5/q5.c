#include <stdio.h>

int main()
{
    printf("------ Questão 5 ------\n");
      /* (a) */
      printf("A respota de valor é 20, uma vez que p1 possui a endereço de Valor, então, *p1 aponta pra Valor\n");

      /* (b) */
      printf("Da mesma forma que o anterior, o valor de temp é 29.0 \n");

      /* (c) */
      printf("No caso, p3 contem o endereço da primeira variavel do char nome, então aux = o valor do primeiro elemento do vetor\n");

      /* (d) */
      printf("p3 contem o endereço de char nome[4], então aux vai receber o valor do 4 elemento\n");

      /* (e) */
      printf("quando atribuimos p3 = nome, quer dizer que p3 vai receber o primeiro valor do char em lower-case\n");

      /* (f) */
      printf("Como no caso anterior o p3 = nome, então como fizemos p3 = p3 + 4, então p3 vai receber o valor do 5 caracter\n");

      /* (g) */
      printf("como estamos subtraindo do valor anterior p3 = p3 + 4, então, a saida vai ser um caracter anterior\n");

      /* (h) */
      printf("uma vez que p4 armazena o valor do endereço de vetor[0] , então idade vai ser o valor do primeiro elemento\n");

      /* (i) */
        printf("p5 vai receber o endereço de p4 +1, logo idade vai recever o valor dp segundo elemento");
      /* (j) */
        printf("p4 vai receber o endereço de p5 + 1, logo idade vai receber o valor do terceiro elemento do vetor");
      /* (l) */
       printf("Como p4 está recebendo o endereço de p4 - 2, idade vai receber o valro do primeiro elemento");
      /* (m) */
        printf("*p5 = vai receber o segundo elemento");
      /* (n) */
        printf("P5 vai receber o 3 elemento");

    return 0;
}
