#include <stdio.h>

int main()
{
    printf("------ Questão 8 ------\n");
    printf("Apenas as três primeiras express~eos estão corretas:\n");
    printf("p = mat + 1, p = mat++ e p = ++mat, pois, p é declarado como um ponteiro.\n");
    printf("Diferente de x, que não é declarado como x, então ele não pode receber o endereço.\n");
    return 0;
}
